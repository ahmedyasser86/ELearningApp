@{
    ViewData["Title"] = "Courses";
    ViewData["ControllerName"] = "UserCourses";
}
@using ELearningApp.Core.Assists
@using ELearningApp.Core.Models
@model PaginatedList<Course>

<div class="d-block" style="overflow-x: auto; width: 100%;">
    <!-- زر إضافة دورة جديدة -->
    <div class="text-end mb-3">
        <a href="/@(ViewData["ControllerName"])/Add" class="btn btn-success">Add New Course</a>
    </div>
    <table id="courses" class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Instructor</th>
                <th>Price</th>
                <th style="width: 50px;"></th>
                <th style="width: 50px;"></th>
                <th style="width: 50px;"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var course in Model.Items)
            {

                <tr>
                    <td>@course.Id</td>
                    <td>@course.Title</td>
                    <td>@course.Instructor?.UserName</td>
                    <td><a class="btn btn-primary py-1 px-2" href="/@(ViewData["ControllerName"])/Edit/@course.Id"><i class="fa fa-pencil"></i></a></td>
                    <td><a class="btn btn-danger py-1 px-2" href="/@(ViewData["ControllerName"])/Delete/@course.Id"><i class="fa fa-trash"></i></a></td>
                    <td><a class="btn btn-info py-1 px-2" href="/@(ViewData["ControllerName"])/Details/@course.Id"><i class="fa fa-eye"></i></a></td>
                </tr>
            }
        </tbody>
    </table>

    <div>
        <!-- أزرار التنقل بين الصفحات -->
        <nav aria-label="Page navigation">
            <ul class="pagination justify-content-center">
                <!-- زر الذهاب لأول صفحة -->
                @if (Model.PageNumber > 1)
                {
                    <li class="page-item">
                        <a class="page-link" asp-action="Index" asp-route-pageNumber="1" asp-route-pageSize="@Model.PageSize">الأولى</a>
                    </li>
                }
                else
                {
                    <li class="page-item disabled">
                        <span class="page-link">الأولى</span>
                    </li>
                }
                <!-- زر السابق -->
                @if (Model.HasPreviousPage)
                {
                    <li class="page-item">
                        <a class="page-link" asp-action="Index" asp-route-pageNumber="@(Model.PageNumber - 1)" asp-route-pageSize="@Model.PageSize">السابق</a>
                    </li>
                }
                else
                {
                    <li class="page-item disabled">
                        <span class="page-link">السابق</span>
                    </li>
                }

                <!-- عرض أرقام الصفحات -->
                @{
                    int startPage = Math.Max(1, Model.PageNumber - 2); // بدء نطاق الأرقام
                    int endPage = Math.Min(Model.TotalPages, Model.PageNumber + 2); // نهاية نطاق الأرقام
                }

                @for (int i = startPage; i <= endPage; i++)
                {
                    <li class="page-item @(Model.PageNumber == i ? "active" : "")">
                        <a class="page-link" asp-action="Index" asp-route-pageNumber="@i" asp-route-pageSize="@Model.PageSize">@i</a>
                    </li>
                }

                <!-- زر التالي -->
                @if (Model.HasNextPage)
                {
                    <li class="page-item">
                        <a class="page-link" asp-action="Index" asp-route-pageNumber="@(Model.PageNumber + 1)" asp-route-pageSize="@Model.PageSize" >التالي</a>
                    </li>
                }
                else
                {
                    <li class="page-item disabled">
                        <span class="page-link">التالي</span>
                    </li>
                }

                <!-- زر الذهاب لآخر صفحة -->
                @if (Model.PageNumber < Model.TotalPages)
                {
                    <li class="page-item">
                        <a class="page-link" asp-action="Index" asp-route-pageNumber="@Model.TotalPages" asp-route-pageSize="@Model.PageSize">الأخيرة</a>
                    </li>
                }
            </ul>
        </nav>
    </div>
</div>
